{
  "name": "campus-marketplace-backend",
  "version": "1.0.0",
  "description": "AWS Lambda backend for Campus Marketplace",
  "type": "module",
  "scripts": {
    "build": "echo 'No build step required for Lambda handlers'",
    "dev": "node src/local-server.js",
    "deploy": "sam build && sam deploy --guided"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "uuid": "^11.0.3"
  }
}
